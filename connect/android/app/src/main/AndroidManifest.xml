<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!--
        Permissions.

        - INTERNET             : Required for OHDC remote storage. The on-device
                                 deployment doesn't need it for storage I/O,
                                 but `Notify.RegisterDevice` (FCM) and OAuth
                                 redirects (Custom Tabs) both need it.
        - POST_NOTIFICATIONS   : Pending review notifications, emergency wake,
                                 case-update notifications. Required on
                                 Android 13+ (API 33+); auto-granted below.
        - USE_BIOMETRIC        : Token unlock via BiometricPrompt — bind the
                                 self-session bearer to user presence.
                                 Implementation TBD; declared so it survives
                                 a future EncryptedSharedPreferences flip
                                 without a manifest edit.
        - WAKE_LOCK            : Foreground service for the Health Connect
                                 bridge will need this; declared early so
                                 the v0 scaffold doesn't lose it on a
                                 manifest reformat.

        FCM full-screen-intent and camera (barcode) come in the
        implementation phase — see `connect/android/BUILD.md` and the
        relevant spec docs at `connect/spec/notifications.md`,
        `connect/spec/screens-emergency.md`, `connect/spec/barcode-scanning.md`.
        Health Connect read permissions are declared just below.
    -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!--
        Foreground-service permissions for the share responder
        (`data/ShareResponderService.kt`). The responder must keep running
        while the app is backgrounded so a doctor / CORD can reach the
        phone's health data — Android 14 (API 34) requires both the generic
        FOREGROUND_SERVICE permission and the type-specific one matching the
        `<service android:foregroundServiceType>` declared below.

        We use `connectedDevice`: the responder holds a persistent QUIC
        tunnel to a relay / remote consumer (the doctor's device). The
        `dataSync` type was rejected — Android 14 caps a `dataSync` foreground
        service at ~6 hours, which would silently kill an always-on
        responder; `connectedDevice` is the closest always-on networked-peer
        type and has no such cap. `specialUse` is the documented fallback but
        requires a Play-review justification string, so the better-fitting
        standard type is preferred.
    -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE" />
    <!-- A `connectedDevice` foreground service additionally requires one of a
         set of connectivity permissions. The share responder holds a network
         tunnel to the relay, so CHANGE_NETWORK_STATE satisfies that gate —
         without it startForeground throws SecurityException on launch. -->
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />

    <!--
        CAMERA — used by the FoodScreen inline barcode preview (CameraX +
        ML Kit). Marked optional via `<uses-feature required="false">` so
        devices without a camera (rare on phones, but Wear / TV variants
        may lack one) still install. Permission is requested at runtime
        the first time the user opens the food scanner.
    -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera.any" android:required="false" />

    <!--
        Health Connect read permissions.

        Each `android.permission.health.READ_*` is a runtime permission
        managed by the Health Connect app (com.google.android.apps.healthdata),
        not by the standard Android permission UI. The strings below mirror
        the constants returned by
        `HealthPermission.getReadPermission(<RecordType>::class)` and must
        appear in the manifest for the Health Connect permission dialog to
        offer them. See `connect/spec/health-connect.md` for the mapping
        from each Health Connect record type to the OHD event type.
    -->
    <!-- Vitals -->
    <uses-permission android:name="android.permission.health.READ_HEART_RATE" />
    <uses-permission android:name="android.permission.health.READ_RESTING_HEART_RATE" />
    <uses-permission android:name="android.permission.health.READ_HEART_RATE_VARIABILITY" />
    <uses-permission android:name="android.permission.health.READ_BLOOD_PRESSURE" />
    <uses-permission android:name="android.permission.health.READ_BLOOD_GLUCOSE" />
    <uses-permission android:name="android.permission.health.READ_OXYGEN_SATURATION" />
    <uses-permission android:name="android.permission.health.READ_RESPIRATORY_RATE" />
    <uses-permission android:name="android.permission.health.READ_BODY_TEMPERATURE" />
    <uses-permission android:name="android.permission.health.READ_BASAL_BODY_TEMPERATURE" />
    <uses-permission android:name="android.permission.health.READ_SKIN_TEMPERATURE" />

    <!-- Body measurements -->
    <uses-permission android:name="android.permission.health.READ_WEIGHT" />
    <uses-permission android:name="android.permission.health.READ_HEIGHT" />
    <uses-permission android:name="android.permission.health.READ_BODY_FAT" />
    <uses-permission android:name="android.permission.health.READ_BODY_WATER_MASS" />
    <uses-permission android:name="android.permission.health.READ_BONE_MASS" />
    <uses-permission android:name="android.permission.health.READ_LEAN_BODY_MASS" />

    <!-- Activity -->
    <uses-permission android:name="android.permission.health.READ_STEPS" />
    <uses-permission android:name="android.permission.health.READ_DISTANCE" />
    <uses-permission android:name="android.permission.health.READ_ELEVATION_GAINED" />
    <uses-permission android:name="android.permission.health.READ_FLOORS_CLIMBED" />
    <uses-permission android:name="android.permission.health.READ_EXERCISE" />
    <uses-permission android:name="android.permission.health.READ_ACTIVE_CALORIES_BURNED" />
    <uses-permission android:name="android.permission.health.READ_TOTAL_CALORIES_BURNED" />
    <uses-permission android:name="android.permission.health.READ_BASAL_METABOLIC_RATE" />
    <uses-permission android:name="android.permission.health.READ_VO2_MAX" />
    <uses-permission android:name="android.permission.health.READ_POWER" />
    <uses-permission android:name="android.permission.health.READ_SPEED" />

    <uses-permission android:name="android.permission.health.READ_STEPS_CADENCE" />
    <uses-permission android:name="android.permission.health.READ_WHEELCHAIR_PUSHES" />

    <!-- Sleep -->
    <uses-permission android:name="android.permission.health.READ_SLEEP" />

    <!-- Nutrition -->
    <uses-permission android:name="android.permission.health.READ_NUTRITION" />
    <uses-permission android:name="android.permission.health.READ_HYDRATION" />

    <!--
        Special access permissions:
        - HEALTH_DATA_IN_BACKGROUND — lets the periodic WorkManager sync read
          while the app isn't foregrounded.
        - HEALTH_DATA_HISTORY — unlocks data older than 30 days (default cap
          for apps installed after the data point's write time). Required
          for our 5-year backfill on first install.
    -->
    <uses-permission android:name="android.permission.health.READ_HEALTH_DATA_IN_BACKGROUND" />
    <uses-permission android:name="android.permission.health.READ_HEALTH_DATA_HISTORY" />

    <!--
        Package visibility queries — Android 11+ filters `PackageManager`
        lookups by default; we have to declare which packages we want to
        see. The Health Connect provider lives in
        `com.google.android.apps.healthdata`; without this entry,
        `HealthConnectClient.getSdkStatus(...)` returns `SDK_UNAVAILABLE`
        even when Health Connect is installed.
    -->
    <queries>
        <package android:name="com.google.android.apps.healthdata" />
    </queries>

    <application
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="false"
        tools:targetApi="33">

        <!--
            Auto-download the ML Kit barcode scanner model on first install.
            Used by the inline CameraX preview in `FoodScreen` /
            `FoodSearchScreen`. Without this, the first scan triggers an
            on-the-fly download with a visible spinner; with it, the model
            is fetched in the background right after install.
        -->
        <meta-data
            android:name="com.google.mlkit.vision.DEPENDENCIES"
            android:value="barcode" />


        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@android:style/Theme.Material.NoActionBar"
            android:launchMode="singleTop"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden|uiMode">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!--
                Gemini-trigger proof-point intent. Fired by:
                  - the static `gemini_test_marker` shortcut (launcher
                    long-press, adb shell `cmd shortcut start-shortcut …`);
                  - Google Assistant / on-device Gemini via the
                    `actions.intent.CREATE_THING` capability declared in
                    `res/xml/shortcuts.xml`.
                MainActivity.onCreate / onNewIntent detects the action and
                drops one `food.eaten` event with `notes = "gemini:marker"`
                so we can verify externally-driven invocations end-to-end
                without the per-domain App Functions surface wired yet.
            -->
            <intent-filter>
                <action android:name="com.ohd.connect.action.GEMINI_TEST_MARKER" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>

            <!-- App Shortcuts + App Actions capability declarations. -->
            <meta-data
                android:name="android.app.shortcuts"
                android:resource="@xml/shortcuts" />
        </activity>

        <!--
            Health Connect "permission rationale" activity-alias.

            Health Connect's permission UI launches this activity (via the
            `VIEW_PERMISSION_USAGE` intent action with the
            `HEALTH_PERMISSIONS` category) whenever the user taps the "Read
            our privacy policy" link inside the permission dialog. We don't
            ship a separate rationale screen yet — for v1 the alias points
            back at `MainActivity`, which lands the user at the in-app
            Health Connect settings screen. The
            `START_VIEW_PERMISSION_USAGE` permission protection means only
            the Health Connect provider can trigger the alias.
        -->
        <activity-alias
            android:name="ViewPermissionUsageActivity"
            android:exported="true"
            android:targetActivity=".MainActivity"
            android:permission="android.permission.START_VIEW_PERMISSION_USAGE">
            <intent-filter>
                <action android:name="android.intent.action.VIEW_PERMISSION_USAGE" />
                <category android:name="android.intent.category.HEALTH_PERMISSIONS" />
            </intent-filter>
        </activity-alias>

        <!--
            FCM receiver for the relay's silent push-wake (relay-protocol.md
            §frame 0x09 WAKE_REQUEST). On a data-only message with
            `category = "tunnel_wake"`, `RelayWakeService` resumes the share
            responders so the relay can complete a consumer's attach. The
            `MESSAGING_EVENT` intent-filter is how FCM dispatches both
            messages and token refreshes; `exported="false"` because only
            the Play Services FCM client binds it.
        -->
        <service
            android:name=".data.RelayWakeService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!--
            Share-responder foreground service (`data/ShareResponderService.kt`).

            Hosts the CORD data-link Phase 4d share responders so the relay
            tunnel survives the app being backgrounded — a doctor / CORD can
            reach the phone's health data without the app in the foreground.
            It runs only while at least one share has remote access enabled
            and stops (removing its notification) once the last is disabled.

            `foregroundServiceType="connectedDevice"` — the responder holds a
            persistent QUIC tunnel to a relay / remote consumer. `dataSync`
            was rejected: its ~6 h Android-14 cap would kill an always-on
            responder. The runtime `startForeground(..., type)` call in the
            service passes the matching `FOREGROUND_SERVICE_TYPE_CONNECTED_DEVICE`.

            `exported="false"` — only the app starts it (via
            `ShareResponders.activate` / `resumeAll`); no external component
            should be able to.
        -->
        <service
            android:name=".data.ShareResponderService"
            android:exported="false"
            android:foregroundServiceType="connectedDevice" />

        <!--
            Full-screen emergency dialog activity and the loud-notification
            channels land in the implementation phase. See:
              - ../spec/notifications.md
              - ../spec/screens-emergency.md
        -->
    </application>
</manifest>
